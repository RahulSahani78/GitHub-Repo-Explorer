# Base origin of the deployed backend API (no trailing slash), e.g.
# https://github-repo-explorer-api.onrender.com
#
# Leave this empty for local development — Vite proxies /api to localhost:4000.
VITE_API_BASE_URL=
